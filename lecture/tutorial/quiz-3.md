# Quiz 3

1. The quadratic cost function is better then linear cost function in the critical path.
2. Multiple short wires are bad for performance because the flexibility increases, but the performance drops. This is always the trend (cannot be both flexible and high-performance)
3. Placement is not sufficient to guarantee routability.
4. Every time if we split a large problem into smaller ones, we will lose some optimality.
